export type User = {
  id: number,
  name: string,
  username: string,
  email: string,
}

export type Post = {
  id: number,
  title: string,
  body: string,
}

export type Album = {
  id: number,
  title: string,
}